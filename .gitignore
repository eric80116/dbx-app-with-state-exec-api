# Python
.venv/
app/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node / frontend
node_modules/
app/frontend/node_modules/
# built SPA (regenerated by `bun run build` / setup.sh)
app/backend/static/

# Databricks bundle state / local config
.databricks/

# Generated, workspace-specific app manifest (setup.sh regenerates it; see app.yaml.example)
app/app.yaml

# Internal build/progress log (kept local; contains workspace-specific identifiers)
PROGRESS.md

# OS / editor
.DS_Store
*.swp
